# Data Directory

Real measurement data is ignored by default.

- `raw/`: raw instrument outputs and untouched traces.
- `processed/`: parsed and cleaned datasets.
- `reports/`: generated Markdown, figures, and summaries.

Commit only small anonymized fixtures when they are required for tests.
